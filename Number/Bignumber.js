// some time we need to resolve some equation with like 5n/2 but that can't understant code. to solve that type of problem we have to need bignumber method

let y = 5n;
let x = Number(y) / 2;
console.log(x);

// if you try x=y/2 that time it's cann't show any result
