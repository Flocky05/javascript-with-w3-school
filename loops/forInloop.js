//ruls of for in loop
// for (key in object) {
// code block to be executed
// }

const numbers = [45, 4, 9, 16, 25];

for (let x in numbers) {
  console.log(`${numbers[x]}`);
}

const a = [1, 2, 3, 4, 5, 6];
a.forEach();
