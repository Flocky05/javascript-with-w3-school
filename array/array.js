// at frist use data type and name the array then use "=" sign and use [ insite ellemnt]
const friendsName = ["mehedi", "emon", "siam", "krishno", "alamin"];
console.log(friendsName);

// length the array

console.log(friendsName.length);

// position the array, as array start with the 0 so every time you have to count the value less one. so use it...

console.log(friendsName[3]);

// position to array value

const postion = friendsName.indexOf("emon");
console.log(postion);

// some time array is also important for any code
// some time array is also important for code
